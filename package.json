{
  "devDependencies": {
    "concurrently": "^9.1.2"
  },
  "scripts": {
    "install:frontend-backend": "npm install && concurrently \"cd frontend && npm install\" \"pip install -r backend/requirements.txt\"",
    "start:frontend-backend": "concurrently \"npm run dev --prefix frontend\" \"python -m uvicorn backend.main:app --host 0.0.0.0 --port 8000 --reload\""
  }
}
